package network;

public class StationPrice {
/*
    Model model;
    Town town;
    List<StationPrice> priceStation;

    public StationPrice(String price, String addres, String latitude, String length) {
    }

    List<StationPrice> getPrices(Town town, GasType type) {
// Do magic on Internet
        return priceStation;


        model.getPrices(town, type, new Response.Listener<List<StationPrice>>() {
            @Override
            public void onResponse(List<StationPrice> prices) {
                onPricesAvailable(prices);
            }
        }, new Response.ErrorListener() {
            @Override
            public void onErrorResponse(VolleyError error) {
                SecondPresenter.this.view.showError(error.getMessage());
            }
        });
    }Gasolinera

*/
}
